# =============================================================================
# Agentic VOC Insight — consolidated Python dependencies
# =============================================================================
#
# Covers all runnable code in this repository:
#   - topic-modelling/topic-modelling-ai.py  (ADK agent, UMAP, HDBSCAN, KeyBERT, Gemini)
#   - topic-modelling/notebook/chrome_viewer.ipynb  (same stack + Jupyter)
#   - vector-db-writer/ingest_bank_feedback.py  (Chroma + Qwen3 or Gemini embeddings)
#   - ai-data-generator/bank_feedback_generator  &  call_center_data  (Gemini JSONL)
#
# Python: 3.10+ recommended (matches ai-data-generator/pyproject.toml).
#
# Install (repo root):
#   conda create -n google-adk python=3.12
#   conda activate google-adk
#   pip install -r requirements.txt
#
# PyTorch: default `torch` is CPU-friendly from PyPI. For CUDA, install the
# matching wheel from https://pytorch.org/ first, then run this file with
# `--no-deps` on torch or comment out the torch line to avoid overwriting.
#
# Subfolder requirements (historical / partial):
#   vector-db-writer/requirements.txt  — OTEL pins + Chroma stack
#   ai-data-generator/requirements.txt — minimal Gemini + YAML only
# This file supersedes those for a single-env full-repo install.
# =============================================================================

# ----- Agent orchestration & Gemini APIs -----
google-adk>=1.0.0
google-genai>=1.0.0

# ----- OpenTelemetry: constrain so chromadb does not pull OTel 1.40+ -----
# (google-adk is sensitive to newer OTel majors; see vector-db-writer/README.md)
opentelemetry-api==1.38.0
opentelemetry-sdk==1.38.0
opentelemetry-proto==1.38.0
opentelemetry-exporter-otlp-proto-common==1.38.0
opentelemetry-exporter-otlp-proto-grpc==1.38.0
opentelemetry-semantic-conventions==0.59b0

# ----- Chroma persistent vector DB -----
chromadb>=0.5.0

# ----- Local embedding models (Qwen3-Embedding-8B) -----
# Qwen3 arch requires transformers>=4.51 (vector-db-writer + topic-modelling KeyBERT)
transformers>=4.51.0
torch>=2.1.0
safetensors>=0.4.0
accelerate>=0.26.0
sentencepiece>=0.1.99

# ----- Topic modelling: geometry, clustering, keywords -----
umap-learn>=0.5.6
hdbscan>=0.8.33
scikit-learn>=1.3.0
scipy>=1.10.0
numpy>=1.24.0
pandas>=2.0.0
pydantic>=2.0.0
keybert>=0.8.0

# ----- Plotting (topic-modelling-ai UMAP review + chrome_viewer notebook) -----
matplotlib>=3.8.0
seaborn>=0.13.0

# ----- YAML config (all components) -----
pyyaml>=6.0.1

# ----- Jupyter (optional but needed to run chrome_viewer.ipynb) -----
jupyterlab>=4.0.0
ipykernel>=6.29.0
